(This plugin no longer requires GATE to be installed in order to be run) 
  Steps to illustrate how this plugin can be used:
  1. set up parameters in build.properties: repositoryLocation, id, and directory where documents will be written
  2. build plugin using 'ant' or using Eclipse
  3. run main method inside RDFToTextTransformer and then look 
     at the results